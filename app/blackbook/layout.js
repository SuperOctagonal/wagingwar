export const metadata = {
  title: 'Horse Blackbook and Watchlist | Waging War',
  description: 'Build a blackbook of horses to watch and get notified when they line up next.',
};

export default function BlackbookLayout({ children }) {
  return children;
}
