export const metadata = {
  title: "Today's Race Cards and Model Rankings | Waging War",
  description: "Browse today's Australian race cards with model-driven runner scores, pace maps, and rankings for every race.",
};

export default function RacesLayout({ children }) {
  return children;
}
