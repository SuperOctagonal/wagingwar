export const metadata = {
  title: "Live Odds Comparison | Waging War",
  description: "Compare best-win prices across bookmakers for every runner in today's races.",
};

export default function OddsLayout({ children }) {
  return children;
}
