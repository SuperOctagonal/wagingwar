export const metadata = {
  title: 'Race Results and Model Accuracy | Waging War',
  description: 'Check past race results and see how accurate the Waging War model rankings were, race by race.',
};

export default function ResultsLayout({ children }) {
  return children;
}
