export const metadata = {
  title: 'Daily Tipping Competition | Waging War',
  description: 'Join daily tipping competitions and compete against other Waging War punters on the leaderboard.',
};

export default function CompetitionsLayout({ children }) {
  return children;
}
